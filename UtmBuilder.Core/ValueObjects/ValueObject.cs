namespace UtmBuilder.Core.ValueObjects
{   
    public abstract class ValueObject
    {
        // a classe é abstrata, porque não faz sentido instanciar um objeto do tipo ValueObject
        // classe abstrata não pode ser instanciada, mas pode ser herdada
        // a classe ValueObject é uma classe base para outras classes

    }
}